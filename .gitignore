.env
*.env
src/
frontend/
pom.xml

# TLS material — never commit private keys or certs
certs/*.key
certs/*.crt
certs/*.pem

# certbot HTTP-01 challenge webroot — populated at renewal time, transient
certbot-webroot/
